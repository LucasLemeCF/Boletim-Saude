events {}

http {
    server {
        listen 8000;

        location ^~ /api {
            proxy_pass http://app:8080;
        }

       location ^~ /api/auth {
            proxy_pass http://app-frontend:3000;
       }

        location / {
            proxy_pass http://app-frontend:3000;
        }
    }
}
